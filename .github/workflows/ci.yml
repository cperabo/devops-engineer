name: CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  build_test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "8.0.x"

      - name: Restore
        run: dotnet restore ./Caplinq.DevOps/Caplinq.DevOps.sln

      - name: Build
        run: dotnet build ./Caplinq.DevOps/Caplinq.DevOps.sln --no-restore -c Release

      - name: Test
        run: dotnet test ./Caplinq.DevOps/Caplinq.DevOps.sln --no-build -c Release --logger "trx;LogFileName=test_results.trx"

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: "**/TestResults/*.trx"
